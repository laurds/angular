import { RoleOnlyDirective } from './role-only.directive';

describe('RoleOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
